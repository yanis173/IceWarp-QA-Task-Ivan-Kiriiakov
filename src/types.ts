/**
 * The three "New" document types offered by the Dashboard's right-click
 * menu (task step 9). Each maps to the menu's `data-key` attribute.
 */
export type NewFileType = "document" | "spreadsheet" | "presentation";
